export interface Testimonial {
  id: number;
  name: string;
  location: string;
  avatar: string;
  text: string;
  skillIcon: string; // Emoji or URL
}

export interface ValueProp {
  id: number;
  icon: string;
  title: string;
  description: string;
  color: string;
}
